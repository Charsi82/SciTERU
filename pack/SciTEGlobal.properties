# Глобальные настройки SciTE (действительны для всех пользователей)

# Учтите, что настройки этого файла могут быть перекрыты файлом SciTEUser.properties
# а также файлами *.properties конкретных языков программирования
# Кроме того, все настройки могут быть модифицированы подключенными скриптами

# Для Linux поместите в $prefix/share/scite
# Для Windows в том же каталоге, где расположен SciTE.EXE (или Sc1.EXE)

# Идея использовать в этом файле секции по праву принадлежит Vadikan <unattended.oszone.net>

[Original]           Настройки из официальной версии программы

# В GTK доступ к панелям редактирования и вывода можно отключить, установив доступность на 0. По умолчанию он равен 1, поэтому включен.
# accessibility=0

# Если эти параметры не пусты, они определяют цвета для использования в списках автозаполнения. Значения по умолчанию зависят от платформы и системных настроек.
# autocomplete.back=#000000
# autocomplete.fore=#80FFA0
# autocomplete.selected.back=#006040
# autocomplete.selected.fore=#00FF40

# Если установлено значение 1 и вызывается список автозаполнения, и в этом списке есть только один элемент, тогда этот элемент выбирается автоматически. Это означает, что соответствующий элемент вставлен, а список не отображается.
# autocomplete.choose.single=1

# Установите количество элементов, отображаемых в списках автозаполнения. По умолчанию 9.
# autocomplete.visible.item.count=15

# Если этот параметр равен 1, то при вводе слова, если только одно слово в документе начинается с этой строки, тогда список автозаполнения отображается с этим словом, поэтому его можно выбрать, нажав Tab.
# autocompleteword.automatic=1

# Этот параметр определяет, будут ли файлы открываться и сохраняться без блокировки пользовательского интерфейса во время их чтения или записи. Файлы большего размера, чем заданный размер в байтах, будут читаться или записываться в фоновом режиме, в то время как файлы меньшего размера будут читаться или записываться напрямую, и SciTE не будет отвечать до тех пор, пока не будет завершен доступ к файлу. Значение по умолчанию -1 разрешает фоновую обработку для всех файлов. Для сохранения используется размер в памяти в байтах, который будет отличаться от размера на диске при использовании кодировки UTF-16.
# background.open.size=20000
# background.save.size-1

# Экспериментальное и недоделанное отображение двунаправленных языков (арабский, иврит и т.д.)
# bidirectional=1

# blank.margin.left=4
# blank.margin.right=4
# Если установлено output.blank.margin.left, то оно переопределяет пустое поле.margin.left для панели вывода.
# output.blank.margin.left=4

# Ширина штриха символа закладки в сотых долях пиксела
bookmark.stroke.width=200

# Стиль маркера закладки
# bookmark.symbol=2

# Цвет дополнительных кареток (при многокареточном вводе)
# caret.additional.fore=#FF00DF

# Толщина рамки вокруг текущей линии
# caret.line.frame=1

# слой отрисовки маркера текущей линии: 0 - непрозрачность, 1- под текстом, 2 - над текстом
# caret.line.layer=1

# стиль каретки: 0 - невидимая, 1- линия, 2 - бокс
# caret.style=1

# Команда установки параметров. должна вызвать функию, печатающиую параметры в формате файла properties/ 
# command.discover.properties=python

# Скрывает окно консоли вызываемое Lua функциями os.execute и lua.popen
create.hidden.console=1

# editor.config.enable=1

# Включает отображение ошибки внутри кода
# error.inline=1

# error.select.line=1

# export.rtf.wysiwyg=0

# Полный путь в заголовке для TEX лексера
# export.tex.title.fullpath=1

# Количество строк вокруг строки с совпадением
filter.context=5

# Цвет строк, содержащих совпадающий текст в фильтре (RGB или RGBA)
# filter.match.back=#FFB00030

# Включает поиск по бинарным файлах
# find.in.binary=1

# Маркеры истории изменений
change.history=3
# indicator.reverted.to.origin.insertion=style:squiggle,colour:#40A0BF
# indicator.reverted.to.origin.deletion=style:point,colour:#40A0BF
# indicator.saved.insertion=style:squiggle,colour:#60A060
# indicator.saved.deletion=style:point,colour:#00A000
# indicator.modified.insertion=style:squiggle,colour:#A0A060
# indicator.modified.deletion=style:point,colour:#A0A000
# indicator.reverted.to.modified.insertion=style:squiggle,colour:#FF8000
# indicator.reverted.to.modified.deletion=style:point,colour:#FF8000
# marker.reverted.to.origin=style:leftrect,fore:#40A0BF,back:#40A0BF
# marker.saved=style:leftrect,fore:#00A000,back:#00A000
# marker.modified=style:leftrect,fore:#A0A000,back:#A0A000
# marker.reverted.to.modified=style:leftrect,fore:#FF8000,back:#FF8000

# Маски файлов и папок, исключаемых из поиска
# find.exclude=*.bak *.orig debug

# Включает поиск в каталогах с названиями, начинающимися с точки
# find.in.dot=1

# find.indicator.incremental=style:compositionthick,colour:#FFB700,under

# стиль индикатора, добавляемого к найденному тексту по кнопке "Пометить все" в диалоге поиска
# find.mark.indicator=style:compositionthick,colour:#FFB700,under

# выделение всех вхождений поиска
# find.strip.incremental=1

# цвет маркеров фолдинга (RGBA)
# fold.back=#808080
# fold.fore=#00DD00

# цвет свернутой линии
# fold.line.colour=#80800080

# ширина обводки маркеров фолдинга в сотых долях пикселя
# fold.stroke.width=200

# качество отрисовки шрифтов(WIN. MAC)
# 0 - дефолт, 1 - не сглаженный, 2 - сглаженный, 3 - оптимизирован под LCD
font.quality=0

# цвет скрытых линий при использовании фильтра
hidden.line.colour=#00800040

# стиль встроенной подсветки текущего слова
# highlight.current.word.indicator=style:roundbox,colour:#0080FF,under,outlinealpha:140,fillalpha:80

# ширина горизонтальной полосы прокрутки
horizontal.scroll.width.tracking=3

# Отложенная стилизация неотображаемой части документа редактора
# 0 - весь документ сразу(по умолчанию)
# 1 - поэтапная стилизация в фоновом режиме
# 2 - невидимая часть после отображаемой стилизуется в фоне
# 3 - невидимые части перед и после отображаемой стилизуется в фоне
idle.styling=0

# автодополнение для IME ввода
# ime.autocomplete=1

# переключает между встроенным (1) и оконным(0) IME
# ime.interaction=1

# Определяет прозрачность индикаторов.
# Может быть переопределено некоторыми индикаторами(find.mark.indicator и т.п.).
# 0 - плоная прозрачность, 255 - полная непрозрачность
# Вне диапазона - используется значение по умолчанию (30).
# indicators.alpha=63

# Индикаторы рисуются над текстом.
# Может быть переопределено некоторыми индикаторами(find.mark.indicator и т.п.).
# indicators.under=1

# список динамических библиотек лексеров, разделённых ';'.
# Специальная запись '.' значение по умолчанию относится к динамической библиотеке «Lexilla»,
# распространяемой вместе с SciTE.
# lexilla.path=.

# not used
# menubar.detachable=1

if PLAT_MAC
    os.x.home.end.keys=1

# output.cache.layout=3
# output.horizontal.scroll.width.tracking=0

# Отложенная стилизация неотображаемой части документа консоли
# 0 - весь документ сразу(по умолчанию)
# 1 - поэтапная стилизация в фоновом режиме
# 2 - невидимая часть после отображаемой стилизуется в фоне
# 3 - невидимые части перед и после отображаемой стилизуется в фоне
# output.idle.styling=1

# Включает отображение панели с полным путем файла на GTK+ 
# pathbar.visible=1

# 1 - двухфазное рисование(по умолчанию)
# 2 - многофазное рисование(не совместим с buffered.draw)
# 0 - устаревший режим, не рекомендуется
# phases.draw=2

# Клавища-модификатор для прямоугольного выделения
# 2 - Ctrl, 4 - Alt, 8 - Super
# rectangular.selection.modifier=8

# 1 - включить добавочный поиск
# 2 - включить добавочный поиск и выделение совпадений
# replace.strip.incremental=1

# Список представлений символов, внешнего вида или цвета
# representations=\x0D\x0A=\u20AA

# Автосохранение по таймеру, 0 - отключено(по умолчанию)
# save.on.timer=20

# Путь для сохранения нового безымянного буфера
# save.path.suggestion=$(SciteUserHome)\note_$(TimeStamp).txt

# Задает цвет выделения при неактивном окне
# selection.inactive.fore=#00A000

# 1 - разрешает множественную вставку при множественном выделении
# 0 - вставка происходит только в последнее выделение
# selection.multipaste=0

# 1 - включает прямоугольное выделение мышью
# selection.rectangular.switch.mouse=1

if PLAT_GTK
    selection.secondary.back=#00800010
# selection.secondary.fore=#A00000

# [GTK] увеличивает ширину сплиттера между редактором и консолью
# split.wide=1

# [GTK] ограничивает высоты кнопок
# strip.button.height=24

# Включает горячие клавиши в окнах замены\поиска. Изменение требует перезапуска редактора.
# strip.shortcuts.enable=0

# Показать индексы вкладок в заголовке программы
# tabbar.hide.index=1

# тип рендеринга
# 0 - устаревший GDI
# 1/2/3 - Direct2D и DirectWrite API с более качественной отрисовкой сглаживания
if PLAT_WIN
    technology=3

# Обычно файлы открываются асинхронно
# Параметр разрешает немедленную загрузку файлов из временной папки
# temp.files.sync.load=1

# Большая панель инстументов (в оригинальном редакторе)
# toolbar.large=1

# [GTK] Использовать стандартные иконки
# toolbar.usestockicons=1

# Чтобы не выделять неактивный код, установите traits.inactive
# traits.inactive=,back:#F0F0F0,eolfilled
traits.inactive=

# Размер символа, отображающего пробел
# whitespace.size=1

[Main]               Различные настройки программы
# Установка каталога SciteUserHome для сохранения пользовательских данных
# (SciTEUser.properties, SciTE.session)
# Работает только в версии Ru-Board
scite.userhome=$(SciteDefaultHome)\home

# Установка в 1 позволяет использовать единый файл настроек, расположенный в корне проекта, вместо нескольких локальных в каждом из подкаталогов
properties.directory.enable=1

# Видимость меню в полноэкранном режиме (невидимо=1)
full.screen.hides.menu=1

# Сворачивать в трей
# minimize.to.tray=1

# После выполнения кода, по F4 выделяет строку - источник ошибки
error.select.line=1

# 0 разрешает прокрутку окна на одну страницу ниже текста
# end.at.last.line=0

# Установка в 1 снижает точность отображения активности/неактивности кнопок на панели инструментов
# Это может улучшить работу на относительно медленных машинах
# undo.redo.lazy=1

# В версии для GTK+ определяет начальные размеры окна диалога выбора файлов вызываемого командами Открыть(Open) и Сохранить(Save). В Windows данный параметр не работает.
# fileselector.width=800
# fileselector.height=600
# fileselector.show.hidden=1

# Начальное значение увеличения (Zoom) размера шрифта, заданного в стиле по умолчанию
# устанавливаемое при запуске SciTE или при изменении разрешения экрана
# Возможно использование как положительных так и отрицательных величин.
# magnification=0
# output.magnification=0

# Вывод данных непосредственно на экран (0) или сначала в буфер, и только потом на экран (1)
buffered.draw=1
# При 1 - более качественный (но и более медленный) способ прорисовки текста
# two.phase.draw=1 //Archaic
# 1 устанавливает цветовую палитру в 8 бит
# use.palette=1

# Checking
# 1 включает запрос сохранения при закрытии файла. О - сохраняет изменения без запроса.
are.you.sure=1

# 1 включает запрос сохранения при сборке (F7) файла. О - сохраняет изменения без запроса.
# are.you.sure.for.build=1
# save.all.for.build=1

# 1 закрывает SciTE при закрытии последней вкладки
# quit.on.close.last=1

# 1 включает слежение за тем, не был ли открытый в SciTE файл модифицирован другой программой
load.on.activate=1

# 1 включает запрос "Файл изменён другой программой. Перезагрузить его?"
are.you.sure.on.reload=0

# 1 включает запрос при сохранении "Файл изменён другой программой. Переписать его?"
save.check.modified.time=1

# 1 включает автоматическое сохранение файла при потере фокуса SciTE
# save.on.deactivate=1

# 1 - при перезагрузке файла не удаляется история отмены
reload.preserves.undo=1

# Если SciTE открыт, то файлы загружаются в существующую копию программы
check.if.already.open=1

# Определяет используемый режим языка для создаваемого (Ctrl+N) файла, прежде чем ему будет назначено имя
default.file.ext=.txt

# Если заданное в ком. строке имя файла без расширения не найдено, то SciTE будет пытаться открыть одноименный файл с указанными ниже расширениями
source.default.extensions=.h|.cxx|.bat

# 1 - диалог "Открыть" открывается в папке текущего файла, 0 - где открывали последний раз
open.dialog.in.file.directory=1

# 1 при сохранении удаляет символы пробелов и табуляции на концах строк
# strip.trailing.spaces=1

# 1 при сохранении добавляет в конец последней строки файла символ окончания строки (если он отсутствует)
# ensure.final.line.end=1

# 1 при сохранении файла устанавливает в окончание всех линий текущий символ окончания строки (CRLF, CR, LF)
ensure.consistent.line.ends=1

# save.deletes.first=1

# При отсутствии перевода вставить знак...
# translation.missing=***

# Открывать новый документ в режиме только чтение
# read.only=1

# max.file.size=1

# При 1 (по умолчанию) переносит каретку как в Visual C++ на конец отступа (начало текста) и если каретка уже там, то на начало строки.
# vc.home.key=0

# Автоматическое создание резервного *.bak файла (auto_backup.lua)
# ключ определяет кол-во сохраняемых вариантов. Отсутствие ключа или 0 - отказ от создания резервной копии
# backup.files=1
# Каталог для сохранения резервных копий (возможно использование переменных окружения)
# При отсутствии параметра резервные копии сохраняются рядом с исходным файлом
backup.path=$(TEMP)\$$$SciTE.tmp
# backup.path=_bak_

# Параметр selection.hide.on.deactivate=0 позволяет не снимать выделение с текста когда окно SciTE теряет фокус.
selection.hide.on.deactivate=0

# При 1 можно делать многократные выделения мышью, удерживая клавишу Ctrl (0 - отключает эту возможность)
# selection.multiple=0

# При 1 печать и вставка текста, нажатие на клавиши BackSpace или Delete будет одновременно применятся на все выделенные части текста
# selection.additional.typing=0

# Открытие файла по клику мышью на его имени с зажатой клавишей Ctrl (Open_Selected_Filename.lua)
open.filename.by.click=1

# Список имён лексеров и список расширений файлов для автоматической подсветки ссылок (HighlightLinks.lua)
# highlight.links.lexers=null
# highlight.links.exts=htm,html,php

# Подсветка всех вхождений текущего слова в документе (встроенная возможность SciTE)
# Совет: Лучше использовать скриптовый вариант (см. ниже). В нем и возможностей - больше и работает быстрее.
# highlight.current.word=1
# highlight.current.word.by.style=1
# highlight.current.word.colour=#00D040

# Авто подсветка текста, который совпадает с текущим словом или выделением (highlighting_identical_text.lua)
highlighting.identical.text=1
# Ограничение на max кол-во вхождений (50 - по умолчанию, 0 - без ограничения).
# highlighting.identical.text.max=100

# Параметр change.overwrite.enable=0 отключает возможность переключения режима вставка/замена
# change.overwrite.enable=0

# Позволяет перемещать каретку дальше последнего символа на линии (1-при прямоугольном выделении, 2-мышью или курсорными клавишами, 3-в обоих случаях)
virtual.space=1

[Position]           Размеры и позиция окна программы
# Позиция левого верхнего угла окна программы
# position.left=0
# position.top=0

# Ширина и высота окна (в полный экран=-1)
# position.width=-1
# position.height=-1
# position.maximize=1

# horizontal.scrollbar=0
horizontal.scroll.width=100

# Если=1, то position.left нового окна = position.left + position.width первого окна.
position.tile=1

[Title]              Текст в заголовке основного окна SciTE
# Выбор, как будет отображаться имя файла в заголовке:
# 0(по умолчанию) -  отображается только имя файла
# 1 - отображается - путь\имя_файла
# 2 - отображается - имя_файла in путь
title.full.path=2

# Отображается ли номер активного буфера и общее кол-во вкладок в заголовке. 
# 0 - не отображается
# 1 - отображается
title.show.buffers=1

[Margin]             Размеры отступов для окна программы, текста, колонки с номерами строк и фолдинга
# 1 - отображать колонку с номерами строк и меток (bookmarks).
line.margin.visible=1

# Резервирует ширину колонки, с точки зрения количества цифр в номере.
# Для автоматического увеличения ширины поля, необходимо добавить + после цифры
line.margin.width=2+

# Размер колонки (в пикселях) для меток(bookmarks). (Требует перезапуска)
margin.width=13

# Размер колонки для фолдинга (свёртывание/развёртывание процедур) (в пикселях)
# fold.margin.width=13

# Размер отступа с обеих сторон текста (в пикселях)
blank.margin.left=4
# blank.margin.right=4

# 0 отключает установку меток по клику мышью в колонке букмарков (только в SciTE-Ru)
margin.bookmark.by.single.click=1

[Output Screen]      Положение и размер окна консоли
# Автоматически очищает окно консоли перед выполнением любого скрипта
# т.е. выполняет команду output:ClearAll()  (LUA)
clear.before.execute=1

# 1 - окно консоли справа, 0 - внизу
split.vertical=0

# Размеры окна консоли
# output.horizontal.size=200
# output.vertical.size=60
# output.initial.hide=1
# output.horizontal.scrollbar=0
# output.horizontal.scroll.width=10000
# output.scroll=0

output.cache.layout=3

# 1 - после завершения команды выводить время её выполнения (в секундах)
time.commands=1

# Размер символа табуляции в окне консоли (используется скрипт в SciTEStartup.lua)
output.tabsize=2

[Tabbar]             Панель вкладок
# Показывать панель вкладок (0/1)
tabbar.visible=1

# Скрывать панель вкладок, если открыт только один файл
tabbar.hide.one=0

# Панель вкладок в несколько строк
tabbar.multiline=1

# Ограничение на длину отображаемого на вкладке имени файла (только в SciTE-Ru)
tabbar.title.maxlength=20

# Включает отображение маркера R/O
read.only.indicator=1

# Символ для отображения статуса R/O в имени вкладки (только в SciTE-Ru)
tabbar.readonly.marker= •

# Закрывать вкладку двойным кликом на ней (только в SciTE-Ru)
tabbar.tab.close.on.doubleclick=1

# Количество вкладок
buffers=100

# Расположение в списке для новой вкладки (только в SciTE-Ru)
# 0 - открывать новую вкладку в конце списка (default), 1 - следующей за текущей, 2 - в начале списка
# buffers.new.position=1

# 1 - переключение (Ctrl+Tab) будет происходить в том порядке, в котором вкладки открывались ранее (иначе - по номеру)
# после закрытия вкладки редактор возвращается к последней открытой до этого (доработка SciTE-Ru)
# buffers.zorder.switching=1

[Braces and Tags]    Поведение скобок и тегов
# Подсветка парных скобок (0/1)
braces.check=1
braces.sloppy=1

# Автозакрытие скобок. (Работает с помощью lua скрипта braces_autoclose.lua)
braces.autoclose=1
braces.open=({['"
braces.close=)}]'"
# braces.multiline=cpp,css,hypertext
braces.multiline=cpp,rust,lua

# Автозакрытие тегов. (Работает с помощью lua скрипта html_tags_autoclose.lua)
tags.autoclose=1

[Whitespace]         Отображение непечатаемых символов (пробелов, табуляции, символов конца строки)
# Показывать символы конца строки
view.eol=0

# Задаёт символ перевода строки (CR, LF или CRLF)
# eol.mode=LF

# Эта установка отменяет eol.mode, и выбирает символ перевода строки исходя из текущего содержания файла
eol.auto=1

# Символ для отображения непечатных символов
# control.char.symbol=°

# Показывать пробелы и табуляции (0/1)
view.whitespace=1
view.indentation.whitespace=1
view.indentation.guides=1
highlight.indentation.guides=1

[Indent]             Настройки отступов и табуляции
# Размер отступа
tabsize=4

# Размер отступа, для использования при автоматических отступах
indent.size=4

# При 0 символ табуляции при вводе автоматически заменяется на пробелы
# use.tabs=0

# При 1 значения indent.size и use.tabs берутся из открытого документа
indent.auto=1

# При 1 символ табуляции при вводе автоматически заменяется на indent.size пробелов
# tab.indents=1

# При 1 нажатие на backspace в пределах отступа удалит indent.size пробелов
backspace.unindents=1

# При 1 - включение автоматического отступа
indent.automatic=1
indent.opening=0
indent.closing=0
# indent.maintain.*=0

[Wrap]               Задание ширины для правой ограничительной линии и перенос длинных строк
# Задание ширины для правой ограничительной линии (если больше - то это считается превышением и может отмечаться)
edge.column=100

# Способ показа превышения допустимой ширины (0-без показа, 1-линия, 2-цвет)
edge.mode=1

# Переносить длинные строки (0/1)
wrap=1

# Переносить длинные строки
# 1 - по словам (по умолчанию)
# 2 - по символам
wrap.style=1

# Флаги для отображения маркеров в конце и начале перенесенных строк для их визуальной идентификации. 
# 0 - не отображать маркеры (по умолчанию).
# 1 - для отображения маркеров в конце перенесенных строк.
# 2 - для отображения маркеров в начале перенесенных строк 
# 3 - для отображения маркеров в начале и в конце.
wrap.visual.flags=3

# Флаги для установки расположения маркеров отображения (если они включены) рядом с текстом или рядом с границей.
# 0 - чтобы маркеры начала и конца располагались рядом с границей (по умолчанию).
# 1 - чтобы маркеры конца располагались рядом с текстом.
# 2 - чтобы маркеры начала располагались рядом с текстом
# 3 - чтобы все маркеры располагались рядом с текстом.
wrap.visual.flags.location=1

# Устанавливает отступ продолжающихся перенесенных строк для лучшего визуального определения переноса.
# По умолчанию 0 (без отступа). Обратите внимание, что если значение wrap.visual.flags равно 2 или 3
# (отображается начальный маркер), строка имеет отступ не менее 1, даже если значение
# wrap.visual.startindent по-прежнему равно 0.
wrap.visual.startindent=4

# Перенесенные подстроки могут иметь различные отступы относительно исходной подстроки.
# 0 - по умолчанию отступает подстроки слева от окна плюс wrap.visual.startindent.
# 1 - выравнивает подстроки по первой подстроке. 
# 2 - выравнивает подстроки по первой подстроке плюс еще один уровень отступа.
wrap.indent.mode=1

# При нажатии на клавиши Home и End курсор позиционируется в строке с учётом переноса
wrap.aware.home.end.keys=1

# Кеширование информации о тексте:
# 0 - отключить кэширование
# 1 - кэшировать строку, на которой стоит курсор ввода
# 2 - кэшировать видимую область страницы и курсор ввода
# 3 - кэшировать весь документ
# Примечание. Чем больше текста подвергается кэшированию, тем больше на это уходит памяти. Используйте для больших файлов.
cache.layout=3

# Переносить длинные строки в консоли (0/1)
output.wrap=1

[Folding]            Свёртывание блоков текста
# Включаем фолдинг (0/1)
fold=1

# Сворачивать при открытии файла (0/1)
# fold.on.open=1

# Автоматическая свёртка всех секций при открытии файлов заданного типа (RestoreRecent.lua)
fold.on.open.ext=properties,ini

# Тип фолдинга (2/4/8/16)
fold.flags=16

# Символ фолдинга (0/1/2/3)
fold.symbols=3

# Подсветка линии фолдинга в текущем блоке. (Работает только при fold.symbols=2/3)
fold.highlight=1

fold.compact=0
fold.comment=1
fold.comment.python=1
fold.quotes.python=1
fold.preprocessor=1
fold.html.preprocessor=0

# Задаёт условный символ обозначающий начало блока свёртки для текстовых файлов. Если вместо символа поставить 1, то началом будут считаться пронумерованные заголовки. (FoldText.lua)
fold.text.outline=*
# fold.text.ext=txt,doc

[Find and Replace]   Поиск и замена
# ------------ ПОИСК В ТЕКУЩЕМ ФАЙЛЕ ------------
# find.in.files.close.on.find=0
# find.replace.matchcase=1
# find.replace.escapes=1
# find.replace.regexp=1
find.replace.regexp.posix=1
# find.replace.wrap=0
# find.replacewith.focus=0

# Если 1, то вместо диалогового окна поиска используется панель
find.use.strip=0
# Если 1, то вместо диалогового окна замены используется панель
replace.use.strip=0

# Если 0, то диалоговое окно поиска не закрывается автоматически
find.close.on.find=0

# 0 отменяет установку меток после нажатия "Пометить все" в диалоге поиска (только в SciTE-Ru)
find.bookmark=0

# Если 1, то при нажатии на кнопку "Заменить все" курсор возвращается на исходную позицию (только в SciTE-Ru)
find.replace.return.to.start=1

# Если 1, то при нажатии на кнопку "Пометить все" удаляются все установленные ранее метки (только в SciTE-Ru)
# (установленные ранее маркеры удаляются всегда)
# find.mark.delete=1

# Позволяет задать способ начального заполнения поля в диалоге поиска (только в SciTE-Ru)
#   0 (по умолчанию) - если есть выделение и оно не содержит переводов строк, то использовать его, иначе взять слово рядом с курсором (как это и было ранее),
#   1 - использовать выделение, если выделения нет, то оставить поле пустым,
#   2 - всегда оставлять поле пустым.
# find.fillout=2

# Включает кнопку "Replace in Buffers" в диалоге замены и checkbox для поиска в определённом стиле
find.replace.advanced=1

# Отметка всех вхождений текста полупрозрачным цветным прямоугольником после нажатия "Пометить все" в диалоге поиска (очень удобно)
find.mark=#0F0F0F

# ------------ ПОИСК В ФАЙЛАХ ------------
# Внутренний поиск, с возможностью рекурсивной обработки, гораздо предпочтительнее внешних инструментов
find.command=
# Утилита findstr имеется на последних версиях Windows, включая 2000
if PLAT_WIN
# 	find.command=findstr /n /s /c:"$(find.what)" $(find.files)
# 	find.input=$(find.what)
if PLAT_GTK
# 	find.command=grep --line-number "$(find.what)" $(find.files)

# Список расширений файлов, в которых будет производится поиск
find.files=\
*|\
*.lua *.properties *.api|\
*.html *.htm *.php *.js|\
*.properties *.html *.xml *.iface|\
*.vbs *.js *.wsf *.hta *.lua|\
make* *.mak *.bat *.cmd *.e|\
*.vb *.vbs *.bas *.frm *.cls *.ctl|\
*.java *.js *.py *.pl *.rb *.cgi *.lua *.conf|\
*.asm *.c *.cc *.cpp *.cxx *.cs *.h *.hh *.hxx *.hpp *.rc|\
*.pas *.dpr *.dpk *.dfm *.res|\
*.idl *.odl *.rc *.rc2 *.dlg *.def

# Параметры скрипта FindText.lua:
# Поиск с учётом регистра
# findtext.matchcase=1
# Отмечать букмарками найденные строки
# findtext.bookmarks=1
# Выводить все найденные строки в консоль
findtext.output=1
# Показывать подсказку по горячим клавишам
findtext.tutorial=1

[Print]              Параметры печати (работает только в Windows)
print.colour.mode=4
# Разница между размером шрифта для печати и размером шрифта, указанным в настройках для стиля по умолчанию

print.magnification=0

# Установка: левый, правый, верхний, нижний отступ, в установленных единицах:
# сотые доли миллиметра или тысячные дюйма
print.margins=500,0,0,0

# Колонтитулы верхний (header) и нижний (footer):
# && = амперсанд (&); &p = номер текущей страницы
# &f = имя файла; &F = полный путь к файлу
# &d = дата файла; &D = текущая дата
# &t = время файла; &T = полное время
print.header.format=$(FileNameExt) — Распечатано $(CurrentDate), $(CurrentTime) — Страница $(CurrentPage)
print.footer.format=$(FilePath) — File date: $(FileDate) — File time: $(FileTime)

# Стиль текста для колонтитулов
print.header.style=font:Arial,size:8,bold,back:#BFBFBF
print.footer.style=font:Arial Narrow,size:8,italics,back:#DFDFDF

[Export]             Параметры экспорта (меню "Файл" - "Экспортировать")
# export.keep.ext=1
export.html.wysiwyg=1
# export.html.tabs=1
# export.html.folding=1
export.html.styleused=1
# export.html.title.fullpath=1
# export.rtf.tabs=1
# export.rtf.font.face=Arial
# export.rtf.font.size=9
# export.rtf.tabsize=8

# Масштабирование (относительно стандартного размера шрифта экрана)
export.pdf.magnification=0

# Шрифт: Courier, Helvetica или Times (при Courier - переносов строки не будет)
export.pdf.font=Courier

# Размер страницы (в точках): ширина, высота.
# Примеры: письмо 612,792; A4 595,842; maximum 14400,14400
export.pdf.pagesize=595,842

# Поля (в точках): left, right, top, bottom
export.pdf.margins=72,72,72,72

export.xml.collapse.spaces=1
export.xml.collapse.lines=1

[Help]               Вызов справки SciTE
command.scite.help="$(SciteDefaultHome)\tools\html_HELP.hta" "$(SciteDefaultHome)\doc\SciTEDoc_rus.html"
command.scite.help.subsystem=2

# Файлы справки для языков программирования подключены в соответствующих файлах .properties

[Warnings]           Звуковые и текстовые предупреждения
# Звуковые предупреждения (работают только на Windows. Содержат указатели на wav файлы. Кириллица в пути не поддерживается)
# ФОРМАТ: <мигание окна (0 - не мигает)>,<путь до звукового файла или частота тона>,<длительность проигрывания (не обязательно) в миллисекундах>
if PLAT_WIN
#     warning.findwrapped=1,C:\WINDOWS\Media\chord.wav
#     warning.notfound=0,C:\WINDOWS\Media\ding.wav
#     warning.wrongfile=0,C:\WINDOWS\Media\Windows Notify.wav
#     warning.executeok=0,C:\WINDOWS\Media\Windows Ding.wav
#     warning.executeok=0,1300,100
#     warning.executeko=1,C:\WINDOWS\Media\Windows Notify.wav
#     warning.nootherbookmark=1,C:\WINDOWS\Media\Windows Notify.wav

# Текстовые сообщения (Alert), если не определены, то не возникают (только в SciTE-Ru)
    # warning.findwrapped.message=Find wrapped!
    warning.notfound.message=Can't find!    
    warning.wrongfile.message=Wrong file name!
    # warning.executeok.message=Execute OK!
    # warning.executeko.message=Execute KO!
    warning.nootherbookmark.message=No other bookmarks!
    # Отменяет вывод предупреждения "Не удаётся открыть файл" (только в SciTE-Ru)
    # warning.couldnotopenfile.disable=1

[StatusBar]          Статусная строка
# Показывать панель состояния (0/1)
statusbar.visible=1

# Кол-во строк состояния. Переключачение между ними - кликом мыши по строке состояния.
statusbar.number=2

# |  Select: $(SelLength) | $(SelHeight) [$(SelectionStart)-$(SelectionEnd)]
statusbar.text.1=\
Line: $(LineNumber) Col: $(ColumnNumber)\
  |  Select: $(SelLength) : $(SelHeight)\
  |  $(OverType)\
  |  $(EOLMode)\
#   |  $(font.current.size)px\
  |  $(ZoomFactorPercent)\
  |  $(scite.lexer.name)\
  |  $(editor.code.page.name)

statusbar.text.2=\
FileName: $(FileNameExt)\
  |  $(NbOfLines) lines, $(BufferLength) bytes\
  |  Create: $(FileDate) $(FileTime)\
  |  FileAttr: $(FileAttr) \
  |  output codepage: $(output.code.page)

[International]      Настройка локали и кодировки
# Имя файла локализации интерфейса
locale.properties=locale-ru.properties
# Кодировка окна редактирования. Изменяется скриптом CodePage.lua. Используется для функций string.{upper|lower|len}
editor.code.page=1251

# Unicode UTF-8
code.page=0

# Russian code.page
# code.page=1251 - Устанавливать НЕ рекомендуется!
character.set=204
output.code.page=0

# OEM DOS866
# code.page=866
# character.set=255

# Required for Unicode to work on GTK+:
# LC_CTYPE=en_US.UTF-8
# output.code.page=65001

# 1 включает перекодировку текста выводимого в окно консоли OEM в ANSI (только в SciTE-Ru)
output.code.page.oem2ansi=0

# 1 включает детектирование кодировки dos866 (CodePage.lua)
code.page.866.detect=0

# Автоматически включает кодировку UTF-8 при открытии UTF-8 файла не имеющего BOM заголовка (только в SciTE-Ru)
# 1 - включает детектирование и автоматически добавляет BOM заголовок к редактируемому тексту,
# 2 - включает детектирование, но не добавляет BOM.
utf8.auto.check=2

# Расширения файлов которые будут создаваться и сохраняться ТОЛЬКО в кодировке UTF-8 (new_file.lua)
file.make.as.utf8=htm,html,py,lua,rs

# Данные переменные используются при задании других параметров (word.characters)
chars.alpha=abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ
chars.numeric=0123456789
# Символы национального алфавита (В SciTE-Ru данная переменная задаётся скриптом CodePage.lua)
# chars.accented=АаБбВвГгДдЕеЁёЖжЗзИиЙйКкЛлМмНнОоПпРрСсТтУуФфХхЦцЧчШшЩщЪъЫыЬьЭэЮюЯя

# Определяет символы, являющиеся частью слов
word.characters.*=$(chars.alpha)$(chars.numeric)$(chars.accented)_

[Context Menu]       Контекстное меню
# Список доступных встроенных команд (IDM_*) см. в файле SciTE\doc\CommandValuesExt_rus.html
# Пользовательская команда = 9000 + номер этой команды в меню Tools

# Контекстное меню редактора
user.context.menu.*=\
Undo|IDM_UNDO|\
Redo|IDM_REDO|\
||\
Cut|IDM_CUT|\
Copy|IDM_COPY|\
Paste|IDM_PASTE|\
Delete|IDM_CLEAR|\
||\
Select All|IDM_SELECTALL|\
Drop Selection|IDM_DROPSELECTION|\
\
Selected|POPUPBEGIN|\
Text -> In a new file|9$(CN_TEXT_IN_NEW_FILE)|\
Block Comment or Uncomment|IDM_BLOCK_COMMENT|\
Calculation/Open URL|9$(CN_SEL_CALC_OPEN)|\
||\
Make Selection Uppercase|IDM_UPRCASE|\
Make Selection Lowercase|IDM_LWRCASE|\
||\
Find in Files|IDM_FINDINFILES|\
Select|POPUPEND|\
||\
Find String/Word|9$(CN_FIND_STRING_WORD)|\
Toggle Bookmark|IDM_BOOKMARK_TOGGLE|\
\
Toggle Fold|POPUPBEGIN|\
Current|IDM_EXPAND|\
All folds|IDM_TOGGLE_FOLDALL|\
Recursive|IDM_TOGGLE_FOLDRECURSIVE|\
Toggle Fold|POPUPEND|\
||\
Close|IDM_CLOSE|

# Контекстное меню вкладки
user.tabcontext.menu.*=\
Close|IDM_CLOSE|\
Close All|IDM_CLOSEALL|\
Close All but Current|9$(CN_CLOSE_ALL_BUT_CURRENT)|\
||\
Открыть содержащую папку|9218|\
||\
Save|IDM_SAVE|\
Save|POPUPBEGIN|\
Save As|IDM_SAVEAS|\
Save a Copy|IDM_SAVEACOPY|\
||\
As HTML|IDM_SAVEASHTML|\
As RTF|IDM_SAVEASRTF|\
As PDF|IDM_SAVEASPDF|\
As LaTeX|IDM_SAVEASTEX|\
As XML|IDM_SAVEASXML|\
Save|POPUPEND|\
Save All|IDM_SAVEALL|\
||\
Move Tab Left|IDM_MOVETABLEFT|\
Move Tab Right|IDM_MOVETABRIGHT|\
||\
Copy To Clipboard|POPUPBEGIN|\
All Text|9$(CN_COPY_TO_CLIPBOARD_ALL)|\
||\
Path/Filename|9$(CN_COPY_TO_CLIPBOARD_FULLNAME)|\
Path|9$(CN_COPY_TO_CLIPBOARD_PATH)|\
Filename|9$(CN_COPY_TO_CLIPBOARD_FILENAME)|\
Copy To Clipboard|POPUPEND|\
||\
Text Size|POPUPBEGIN|\
Magnify|9$(CN_TEXT_SIZE_MAGNIFY)|\
Reduce|9$(CN_TEXT_SIZE_REDUCE)|\
Restore to Normal|9$(CN_TEXT_SIZE_NORMAL)|\
Text Size|POPUPEND|\
Encoding|POPUPBEGIN|\
Code Page Property|IDM_ENCODING_DEFAULT|\
UCS-2 Big Endian|IDM_ENCODING_UCS2BE|\
UCS-2 Little Endian|IDM_ENCODING_UCS2LE|\
UTF-8 with BOM|IDM_ENCODING_UTF8|\
UTF-8|IDM_ENCODING_UCOOKIE|\
||\
dos866 <-> win1251|9$(CN_CONVERT_OEM2WIN)|\
Encoding|POPUPEND|\
||\
Rename current file|9$(CN_RENAME_CURRENT_FILE)|\
Open in new window|9$(CN_OPEN_BUFFER_IN_NEW_WINDOW)|\
||\
Print|IDM_PRINT|

# Контекстное меню консоли
user.outputcontext.menu.*=\
Undo|IDM_UNDO|\
Redo|IDM_REDO|\
||\
Cut|IDM_CUT|\
Copy|IDM_COPY|\
Paste|IDM_PASTE|\
Delete|IDM_CLEAR|\
||\
Select All|IDM_SELECTALL|\
Drop Selection|IDM_DROPSELECTION|\
Clear Output|IDM_CLEAROUTPUT|\
||\
Кодировка консоли|POPUPBEGIN|\
Win1251|9250|\
UTF-8|9251|\
Codepage|POPUPEND|\
||\
Find String/Word|9$(CN_FIND_STRING_WORD)|\
Selected text -> In a new file|9$(CN_TEXT_IN_NEW_FILE)|\
||\
Hide|IDM_TOGGLEOUTPUT|

[Shortcuts]          Горячие клавиши пользователя
# Список доступных встроенных команд (IDM_*) см. в файле SciTE\doc\CommandValuesExt_rus.html
# Пользовательская команда = 9000 + номер этой команды в меню Tools
user.shortcuts=\
Ctrl+PageUp|IDM_PREVFILE|\
Ctrl+PageDown|IDM_NEXTFILE|\
Ctrl+Shift+V|IDM_PASTEANDDOWN|\
Ctrl+F12|IDM_WRAP|\
Alt+F11|IDM_NEXTFONTSET|\
# Ctrl+Shift+KeypadMultiply|IDM_TOGGLE_FOLDALL|\
Ctrl+Shift+Num *|IDM_TOGGLE_FOLDALL|\
Alt+R|IDM_READONLY|\
Ctrl+Shift+PageUp|IDM_MOVETABLEFT|\
Ctrl+Shift+PageDown|IDM_MOVETABRIGHT|\
# Ctrl+Shift+B|IDM_HELP|\
Ctrl+Shift+M|IDM_MACROLIST|

[ToolBar]            Настройка панели инструментов используемая по умолчанию
# Показывать панель кнопок (0/1)
toolbar.visible=1

# Параметр устанавливает панель инструментов в формате:
# всплывающая подсказка | IDM_ константа или её номер в меню Tools + 9000 | номер иконки
user.toolbar.*=\
|||\
New|IDM_NEW|0|\
Open|IDM_OPEN|1|\
Save|IDM_SAVE|2|\
Save All|IDM_SAVEALL|30|\
|||\
Print|IDM_PRINT|3|\
|||\
Cut|IDM_CUT|4|\
Copy|IDM_COPY|5|\
Paste|IDM_PASTE|6|\
Delete|IDM_CLEAR|7|\
|||\
Undo|IDM_UNDO|8|\
Redo|IDM_REDO|9|\
|||\
Find|IDM_FIND|10|\
Replace|IDM_REPLACE|11|\
|||\
Go|IDM_GO|16|\
Build|IDM_BUILD|17|\
Compile|IDM_COMPILE|18|\
|||\
Run Current Macro|IDM_MACROPLAY|13|\
Stop Recording Macro|IDM_MACROSTOPRECORD|14|\
Record Macro|IDM_MACRORECORD|15|\
|||\
End of Line|IDM_VIEWEOL|19|\
Change Fonts|IDM_MONOFONT|21|\
Read-Only|IDM_READONLY|22|\
Wrap|IDM_WRAP|29|\
|||\
Output|IDM_TOGGLEOUTPUT|23|\
Full Screen|IDM_FULLSCREEN|24|\
|||\
Open Global Options File|IDM_OPENGLOBALPROPERTIES|26|\
Open User Options File|IDM_OPENUSERPROPERTIES|27|\
Open Abbreviations File|IDM_OPENABBREVPROPERTIES|28|\
|||\
Help|IDM_HELP|25

# Подключение пользовательской панели инструментов (она заменит заданную выше встроенную!)
# import toolbar\toolbar_gnome.properties
import toolbar\toolbar_cool.properties

[Styles]             Шрифты и цветовые настройки
# Задание пользовательских переменных для шрифтов, используемых в стандартных стилях.
# -----------------------------------------------------------
# Переключение между наборами производится комбинацией Ctrl+F11 с помощью скрипта FontChanger.lua
# Первая подборка шрифтов (фиксированный)
font.0.base=font:Courier New,size:10
font.0.small=font:Courier New,size:8
font.0.comment=font:Courier New,size:9,italics

# Вторая подборка шрифтов (пропорциональный)
font.1.base=font:Verdana,size:10
font.1.small=font:Microsoft Sans Serif,size:8
font.1.comment=font:Comic Sans MS,size:8

# Третья подборка шрифтов (фиксированный)
# font.2.base=font:Consolas,size:10
# font.2.small=font:Consolas,size:8
# font.2.comment=font:Consolas,size:8,italics

# Определяем какой из наборов шрифтов будет использоваться по умолчанию
font.base=$(font.0.base)
font.small=$(font.0.small)
font.comment=$(font.0.comment)

# Переменная font.monospace часто используется в файлах языков
# а также используется в скрипте SciTEHexEdit.lua
font.monospace=$(font.base)
# -----------------------------------------------------------

# Зададим типовые цвета, чтобы затем их использовать во всех языковых .properties.
colour.comment=fore:#008000
colour.code.comment.doc=$(colour.comment),back:#FEFEFE
colour.embedded.comment=back:#E0EEFF

colour.number=fore:#AC00A9
colour.keyword=fore:#0000DF
colour.string=fore:#5E5EAE
colour.char=$(colour.string)
colour.operator=fore:#FF0000
colour.preproc=fore:#7F7F00
colour.error=fore:#FFFF00,back:#FF0000
colour.whitespace=fore:#808080

# Возможные значения параметров для строки style:
# font:<name>, size:<number>, fore:<colour>, back:<colour>, italics|notitalics, bold|notbold, eolfilled|noteolfilled, underlined|notunderlined, case:m|u|l, visible|notvisible, changeable|notchangeable
# Если строке стиля параметр упоминается дважды (следует избегать!), то действует крайнее правое значение.

# Глобальные стили по умолчанию для всех языков программирования

# Заданный по умолчанию стиль, и его особенности будут унаследованы всеми другими стилями
# style.*.32=$(font.base),fore:#AC00A9,back:#FFFF00
style.*.32=$(font.base)

# Цвет фона номеров строк в режиме только для чтения (используется скриптом ReadOnly.lua)
# Если не задан, то фон в режиме ReadOnly не меняется
style.back.readonly=#FFAAAA

# Стиль для номеров строк (с левого края)
style.*.33=font:Microsoft Sans Serif,size:8,notbold

# Стиль используется, чтобы отобразить парные скобки () или {}
style.*.34=back:#99FFCC,bold

# Стиль используется, чтобы отобразить незакрытые скобки ( или {
style.*.35=back:#FFAAFF,bold
# Примечание: Для того, чтобы видеть фоновый цвет в style.*.34 и style.*.35, необходимо либо запретить caret.line.back либо использовать прозрачность (caret.line.back.alpha)

# Стиль для непечатных символов. Это не полный стиль. И передний план, и фоновые цвета для этих символов определены соответствующим Lexer-ом, а не этим стилем
style.*.36=

# Indentation guides
style.*.37=fore:#C0C0C0

# Стиль всплывающих подсказок по синтаксису (берутся из API файлов)
style.*.38=font:MS Sans Serif,size:9

# -------------------------------------------------------------
# Колонка для фолдинга (свёртывание/развёртывание процедур)
# Пиксели фона колонки закрашиваются в шахматном порядке двумя цветами
# Первый цвет "шахматной доски"
fold.margin.colour=#ECE9D8
# Второй цвет "шахматной доски"
# fold.margin.highlight.colour=#0000FF
# Цвет вертикальной линии фолдинга блока, в котором стоит курсор
fold.highlight.colour=#0099FF

# Цвет завышенных по длине строк (или ограничительной линии)
edge.colour=#FFE8FF

# Цвет выделенного текста
# selection.fore=
# Значение #DADADA даёт стандартный серебрянный C0C0C0
# на дисплеях с малым числом цветов и светло серый при большей глубине цвета
# selection.alpha=20 // Archaic
# selection.back=#00000030

selection.additional.back=#00000030
selection.inactive.back=#00000030

# selection.additional.fore=#0000A0
# selection.additional.back=#000080
# selection.additional.alpha=50 // Archaic

# Пробельные символы (пробел, табуляция)
whitespace.fore=#8F8F8F
# whitespace.back=#FFF0F0

# Цвет обода метки
# bookmark.fore=#000000

# Фоновый цвет метки
# bookmark.back=#AA0000
bookmark.alpha=25

# Цвет обода метки на строку с источником ошибки (значение по умолчанию #7F0000)
# error.marker.fore=#FF0099

# Фоновый цвет метки, строки в коде с источником ошибки, и строки в консоли с текстом ошибки (значение по умолчанию #FFFF00)
# error.marker.back=#FF9999

# Фоновый цвет строки с текстом ошибки в окне консоли (если необходим цвет, отличный от error.marker.back) (только в SciTE-Ru)
# error.line.back=#330000

# Курсор
caret.fore=#0000FF

# Цвет подсветки линии с курсором
caret.line.back=#0099FF

# Прозрачность (от 0 до 255) символов на линии с курсором
caret.line.back.alpha=30  

# Курсор в окне консоли (только в SciTE-Ru)
output.caret.fore=#0000FF

# Цвет подсветки линии с курсором в окне консоли (только в SciTE-Ru)
output.caret.line.back=#0022FF

# Прозрачность (от 0 до 255) символов на линии с курсором в окне консоли (только в SciTE-Ru)
output.caret.line.back.alpha=40

# Стили маркеров выделения текста, используемые в скриптах (от 0 до 31)
# Формат строки значения: <цвет>, <стиль>, <прозрачность>, где
# <стиль> может принимать значения:
#  plain, squiggle, tt, diagonal, strike, hidden, roundbox, box
# <прозрачность> = @<число>, прозрачность заливки для стиля box, roundbox, dotbox, straightbox, fullbox число от 0 до 255
# <прозрачность границы> = $<число>, прозрачность границы для стиля box, roundbox, dotbox, straightbox, fullbox число от 0 до 255
# Если цвет не задан, используется цвет маркера find.mark по умолчанию
# Если стиль не задан, используется roundbox

# для paired_tags.lua:
indic.style.11=#0000FF
indic.style.12=#FF0000
# для HighlightLinks.lua:
indic.style.13=#0000FF,plain
# для highlighting_identical_text.lua:
indic.style.14=#FF9900,box,$250
indic.style.15=#FF0000,box
# для highlighting_text.lua:
highlighting.text.marker=16
indic.style.16=#FF0080,@120
# для FindText.lua:
findtext.first.mark=27
indic.style.27=#CC00FF,@50
indic.style.28=#0000FF,@50
indic.style.29=#00FF00,@50
indic.style.30=#FFFF00,@100
indic.style.31=#11DDFF,@80

[Caret]              Курсор
# Скорость мерцания курсора (в миллисекундах)
caret.period=500

# Мерцание во всех выделениях
# caret.additional.blinks=0

# Ширина курсора
# caret.width=2

caret.policy.xslop=1
caret.policy.width=20
caret.policy.xstrict=0
caret.policy.xeven=0
caret.policy.xjumps=0
caret.policy.yslop=1
caret.policy.lines=1
caret.policy.ystrict=1
caret.policy.yeven=1
caret.policy.yjumps=0
# visible.policy.strict=1
# visible.policy.slop=1
# visible.policy.lines=4
# caret.sticky=1

[Abbrev & API]       Параметры автодополнения и всплывающих подсказок
# При 1 SciTE после ввода каждой буквы будет услужливо выводить подсказку с возможными вариантами дополнения.
autocompleteword.automatic=1

# При 1 подсказка будет многовариантной (только в SciTE-Ru)
autocompleteword.incremental=1

# (индивидуальные параметры для языков см. в соответствующих .properties)
autocomplete.*.ignorecase=1
calltip.*.ignorecase=1

# Параметры скрипта abbrevlist.lua:
# При 1 метки табуляторов не очищаются после перемещения на них по Tab. Их пользователь удаляет вручную комбинацией Ctrl+Tab.
# abbrev.multitab.clear.manual=1
# Стиль для отображения меток (по умолчанию метки невидимы)
# abbrev.multitab.indic.style=#FF6600,diagonal
# Максимальная ширина раскрывающегося списка расшифровок аббревиатур
abbrev.list.width=80

[Menu Language]      Настройка меню "Подсветка" (переключение алгоритма распознавания содержимого различных типов файлов)
# В файле настроек языка добавляется :
# для добавления в фильтр диалога открытия/сохранения файла
# *filter.lang_name=$(filter.lang_name);
# для добавления в список Исходные тексты диалога открытия/сохранения файла
# *source.patterns.lang_name=$(filter.lang_name);
# для добавления в меню Подсветка
# *language.lang_name=строка меню | расширение файла | клавиша|
# Каждый пункт меню состоит из трёх частей: строка меню | расширение файла | клавиша
# Разрешены функциональные (Fn) и буквенные клавиши в комбинации с Ctrl и/или Shift:
# [Ctrl+][Shift+][Fn|a]. Примеры: F12, Ctrl+Shift+D.
# Используйте '&' перед символом, чтобы создать акселератор.

[Languages]          Импорт всех специализированных файлов настройки для конкретных языков программирования
# Список файлов properties, которые разрешено импортировать
# imports.include=
# Это свойство останавливает импорт перечисленных файлов.Проверяется, только если imports.include пусто или отсутствует.
# imports.exclude=abaqus ada asciidoc asl asm asn1 au3 ave avs avr baan blitzbasic bullant caml cil cmake cobol coffeescript csound d dataflex diff ecl eiffel erlang escript flagship forth fortran freebasic fsharp gap haskell hex hollywood inno kix latex lisp lot lout maxima metapost mmixal modula3 nim nncrontab nsis opal oscript pov powerpro ps purebasic r raku rebol rust sas scriptol smalltalk sorcins spice specman sql tacl tal txt2tags verilog vhdl visualprolog
# Свойства imports.include и imports.exclude имеет смысл использовать только тогда, когда все подключаемые файлы лежат в одной папке с программой.

# Файлы, в конце имени которых стоит символ подчёркивания немного отличаются от распространяемых с официальным дистрибутивом
# Закомментируйте неиспользуемые вами языки!
# import languages\abap
# import languages\abaqus
# import languages\ada
# import languages\ahk
# import languages\alda
# import languages\asciidoc
# import languages\asm
# import languages\asn1
# import languages\au3
# import languages\ave
# import languages\avr
# import languages\avs
# import languages\baan
# import languages\blitzbasic
# import languages\bullant
# import languages\caml
# import languages\cmake
# import languages\cobol
# import languages\conf
import languages\cpp
# import languages\csound
# import languages\css
# import languages\d
# import languages\ecl
# import languages\eiffel
# import languages\erlang
# import languages\escript
# import languages\euphoria
# import languages\flagship
# import languages\forth
# import languages\fortran
# import languages\freebasic
# import languages\gap
# import languages\haskell
# import languages\hollywood
import languages\html
# import languages\inno
# import languages\kix
# import languages\latex
# import languages\lisp
# import languages\lot
# import languages\lout
import languages\lua
# import languages\matlab
# import languages\metapost
# import languages\mmixal
# import languages\modula3
# import languages\nim
# import languages\nncrontab
# import languages\nsis
# import languages\opal
# import languages\others
# import languages\pascal
# import languages\perl
# import languages\php.keywords
# import languages\pov
# import languages\powerpro
# import languages\powershell
# import languages\ps
# import languages\purebasic
import languages\python
# import languages\r
# import languages\rebol
import languages\registry
import languages\rust
# import languages\ruby
# import languages\scriptol
# import languages\smalltalk
# import languages\sorcins
# import languages\specman
# import languages\spice
# import languages\sql
# import languages\tacl
# import languages\tal
# import languages\tcl
# import languages\tex
import languages\text
# import languages\txt2tags
# import languages\vb
# import languages\verilog
# import languages\vhdl
# import languages\yaml
# Другие неучтённые языки (*.properties;*.abbrev;*.ini;*.inf;*.reg;.txt;*.log;*.lst;*.doc;*.bat;*.cmd; и др.)
import languages\others

# Маска $(star *language.) подхватывает часть встроенных настроек
# поэтому мы используем ручной выбор
menu.language=\
Text|txt|$(keyText)|\
# Ada|ads||\
# ABAP|abap||\
# Asciidoc|adoc||\
# AutoIt3|au3||\
# AutoHotkey|ahk||\
# Apache Confi&g|conf||\
# Atmel AVR|avr||\
# Assembler|asm||\
# ASN.1|asn1||\
# Avenue|ave||\
# Baan|bc||\
# Basic|bas||\
&Batch|bat||\
# Bullant|ant||\
&C / C++|c||\
CMake|cmake||\
# C&#|cs||\
# COBOL|cob||\
# Csound|orc||\
# CSS|css||\
# D|d||\
# &Difference|diff||\
# &Eiffel|e||\
# Erlang|erl||\
# &Errorlist|err||\
# Euphoria|e||\
# FlagShip|prg||\
# Forth|forth||\
# &Fortran|f90||\
# Gap|g||\
# Haskell|hs||\
H&ypertext|html|$(keyHTML)|\
# &InnoSetup|iss||\
# &Java|java||\
# Java&Script|js|$(keyJS)|\
# &Kix|kix||\
# Lisp|lisp||\
# Lot|lot||\
# Lout|lt||\
Lu&a|lua||\
# Matlab|m.matlab||\
&Makefile|mak|$(keyMake)|\
# MetaPost|mp||\
# MMIXAL|mms||\
# Modula-3|m3||\
# &nnCron crontab|tab||\
# NSIS|nsi||\
# Objective Caml|ml||\
# Octave|m.octave||\
# Opal|impl||\
# Pascal|pas||\
# Pe&rl|pl||\
# P&HP|php||\
# P&LSQL|spec||\
# P&ostScript|ps||\
# P&OV-Ray SDL|pov||\
# PowerShell|ps1||\
# PowerPro|powerpro||\
&Properties|properties||\
Pytho&n|py||\
# R|R||\
Registry|reg||\
# Reso&urce|rc||\
# Ruby|rb||\
# Shell|sh||\
# S&QL|sql||\
# Specman|e||\
# &TCL|tcl||\
# TeX|tex||\
# &txt2tags|t2t||\
# &VB|vb||\
# VBScr&ipt|vbs||\
# Verilog|v||\
# VHDL|vhd||\
&XML|xml|$(keyXML)|\
# YAML|yaml||

menu.language=$(star *language.)

[Open]               Настройки файлового фильтра в диалогах открытия и сохранения файла
# Собираем все настройки с префиксом "*source.patterns." из всех активных файлов .properties
source.files=$(star *source.patterns.)
source.all.filter=All Source|$(source.files)|

all.files=All Files (*.*)|*.*|
top.filters=$(source.all.filter)$(all.files)

# Собираем все настройки с префиксом "*filter." из всех активных файлов .properties
list.filters=$(star *filter.)

open.filter=$(top.filters)$(list.filters)

# По умолчанию
# save.filter=$(open.filter)

[LUA]                Подключение при загрузке внешнего LUA скрипта
ext.lua.startup.script=$(SciteDefaultHome)\tools\SciTEStartup.lua
# ext.lua.auto.reload=0
# ext.lua.reset=1

[Macros]             Макросы
# Путь до файла макросов (по умолчанию $(scite.userhome)\SciTE.macro или $(SciteDefaultHome)\SciTE.macro)
# macro.file.path=

# Считывать макросы из файла макросов при запуске редактора
macro.load.on.startup=1

# Автоматически записывать текущие макросы в файл макросов
macro.autosave=1

# Открывать диалог задания имени макроса после окончания его записи (необходима внешняя библиотека shell)
macro.fill.name.dialog=1

# Позиция в списке макросов для вновь записанного (по умолчанию =1)
# -1 - задаёт добавление в конец списка
# macro.new.record.position=-1

# Стиль  для номеров строк (с левого края) во время записи макроса
macro.recording.numfield.style=$(style.*.33),back:#E9B6C9

[SideBar]            Настройки многофункциональной боковой панели (SideBar.lua)
# Использовать для боковой панели отдельное окно: 1-да, 0-нет(default)
# sidebar.win=1
sidebar.win=0
# Показывать SideBar при запуске редактора: 1-да, 0-нет(default)
sidebar.show=1
# Позиция и ширина панели
# sidebar.position=left
# sidebar.width=300
# Показывать флаги функций: 1-да, 0-нет(default)
# sidebar.functions.flags=1
# Показывать параметры функций: 1-да, 0-нет(default)
# sidebar.functions.params=1
# Cпособ предпросмотра аббревиатур: 1-calltip, 0-annotation(default)
sidebar.abbrev.calltip=1
# Стиль для аннотаций
style.*.255=fore:#808080,back:#FEFEFE

[Session & Settings] Опции сохранения установок и работы с сессиями
# Сохранять список недавно запущенных файлов (в меню Файл)
save.recent=1

# При запуске SciTE открывать файлы с которыми работали при прошлом сеансе
save.session=1

# Сохранять в файле сессии положение меток
session.bookmarks=1

# Сохранять в файле сессии фолдинг
session.folds=1

# Сохранять в файле сессии позицию и размеры окна SciTE
save.position=1

# Сохранять в файле сессии историю Поиска и Замены
save.find=1

# Записывать файл сессии при выходе только если открыто более одной вкладки (только в SciTE-Ru)
# save.session.multibuffers.only=1

# Max кол-во пунктов (до 30) в списке последних открывавшихся файлов (только в SciTE-Ru)
# save.recent.max=15

# Дополнительно использовать файл SciTE.recent (для хранения информации о ВСЕХ открываемых файлах) (RestoreRecent.lua и RestoreRecent.js)
save.session.recent=1

# Восстановление сессии при открыти одного файла
restore.session.onload=1

# Использовать SessionManager.hta для открытия и сохранения сессий
# session.manager=1
# При закрытии SciTE открывать диалог SessionManager.hta для сохранения текущей сессии
# save.session.manager.on.quit=1
# При закрытии SciTE автоматически создавать файл "currentfilename_num.session" с содержимым текущей сессии
# save.session.on.quit.auto=1

# Задаёт закрывать или нет уже открытые файлы при загрузке сессии (только в SciTE-Ru)
# по умолчанию = 1, и все вкладки перед загрузкой закрываются
# session.close.buffers.onload=0

# Сохранять в файле сессии некоторые установки SciTE (save_settings.lua)
save.settings=1

# Делаем доступными переменные из файла SciTE.session
import home\SciTE.session

[Tools Menu]         Пользовательские команды меню "Tools"
import SciTEToolsMenu.properties
